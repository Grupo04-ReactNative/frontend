import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    header: {
        fontSize: 30,
        fontWeight: 'bold',
        padding: 20,
      },
        
  });

  export { styles };