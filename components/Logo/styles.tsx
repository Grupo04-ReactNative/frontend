import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    logoContainer: {
        justifyContent: "flex-end",
        alignItems: "center",
        marginBottom: 60
    },
    logoText: {
        fontSize: 24,
        fontWeight: "bold",
        color: "#fff",
        marginTop: 10
    },

    logoImage: {
        width: 320,
        height: 320
    },
})