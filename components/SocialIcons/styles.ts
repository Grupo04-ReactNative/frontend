import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    icons: {
        flex: 1,
        flexDirection: 'row',
        width: 212,
        justifyContent: "space-around",
        alignItems: "center"
    },
});